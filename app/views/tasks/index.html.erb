<h1>My Tasks</h1>
<% @tasks.each do |task| %>
<ul>
  <li><%= check_box_tag "task.completed", '1', task.completed %>
    <%= link_to task.title, task_path(task) %></li>
</ul>
<% end %>

<%= link_to "Add a Task", new_task_path %>
